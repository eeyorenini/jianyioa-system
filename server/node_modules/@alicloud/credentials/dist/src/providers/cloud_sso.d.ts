import CredentialsProvider from '../credentials_provider';
import { Session, SessionCredentialProvider } from './session';
declare class CloudSSOCredentialsProviderBuilder {
    signInUrl: string;
    accountId: string;
    accessConfig: string;
    accessToken: string;
    accessTokenExpire: number;
    connectTimeout?: number;
    readTimeout?: number;
    withSignInUrl(signInUrl: string): this;
    withAccountId(accountId: string): this;
    withAccessConfig(accessConfig: string): this;
    withAccessToken(accessToken: string): this;
    withAccessTokenExpire(accessTokenExpire: number): this;
    withConnectTimeout(connectTimeout: number): this;
    withReadTimeout(readTimeout: number): this;
    build(): CloudSSOCredentialsProvider;
}
export default class CloudSSOCredentialsProvider extends SessionCredentialProvider implements CredentialsProvider {
    private readonly signInUrl;
    private readonly accountId;
    private readonly accessConfig;
    private readonly accessToken;
    private readonly accessTokenExpire;
    private readonly connectTimeout;
    private readonly readTimeout;
    private doRequest;
    static builder(): CloudSSOCredentialsProviderBuilder;
    constructor(builder: CloudSSOCredentialsProviderBuilder);
    getProviderName(): string;
    getCredentialsInternal(): Promise<Session>;
}
export {};
